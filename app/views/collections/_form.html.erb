<%= form_for([@user, Collection.new], :class => "span6 well") do |f| %>
  
    
  
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
